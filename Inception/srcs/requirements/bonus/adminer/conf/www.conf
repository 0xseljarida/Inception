; Overrides the packaged 'www' pool.
; php-fpm merges sections with the same name, and this file is read after
; www.conf, so 'listen' below wins.
[www]

; Accept FastCGI requests over TCP instead of the packaged unix socket,
; so nginx can reach php-fpm from another container.
listen = 0.0.0.0:9000
